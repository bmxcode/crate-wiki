# Index

Every page in this wiki, exactly once, under its type, as `[[Page Name]] — one line`.

A page that isn't here is lost, whatever else was done right.

<!-- Everything below is generated by `crate index`, which reads each page's `summary:`
     frontmatter. Don't edit it by hand: the next regeneration discards what you wrote.
     A page's one-liner belongs on the page, not here. -->
