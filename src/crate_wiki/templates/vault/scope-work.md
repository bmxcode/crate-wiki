## This vault is work scope

This vault holds client work — proprietary code, pasted email and Slack. It has **no git remote**,
and `git push` is refused. That's deliberate, not a misconfiguration to fix.

Never suggest adding a remote, syncing, or copying anything out of here. Any work→personal
transfer is a manual step I make deliberately, and there is no version of it that's your idea.
