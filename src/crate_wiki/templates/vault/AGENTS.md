# AGENTS.md

The schema for this vault is [CLAUDE.md](CLAUDE.md). Read it now, before anything else.

It's the whole instruction set — page types, conventions, and what you may and may not write.
This file exists only because you don't read `CLAUDE.md` by default.
