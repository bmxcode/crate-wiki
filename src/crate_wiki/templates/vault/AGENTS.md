# AGENTS.md

The schema for this vault is [CLAUDE.md](CLAUDE.md), and this vault's own conventions are in [CONVENTIONS.md](CONVENTIONS.md). Read both now, before anything else.

Between them they're the whole instruction set — page types, conventions, and what you may and may not write. This file exists only because you don't read `CLAUDE.md` by default.
